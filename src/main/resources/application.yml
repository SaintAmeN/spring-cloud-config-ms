spring:
  cloud:
    config:
      server:
        git:
          uri: http://bitbucket.aps-drones.com:7990/scm/ass/configuration.git
          username:
          password:

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:port/eureka/